# StarkNet Deploy

A simple StarkNet contract deployment tool.

## Overview

This tool helps you deploy contracts to the StarkNet with ease.

## Install

To install the tool, run:

```sh
npm install starknet-deploy
```
